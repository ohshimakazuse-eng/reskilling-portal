services:
  - type: web
    name: reskilling-portal
    env: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
      - key: HOST
        value: 0.0.0.0
      - key: PORT
        value: 4173
      - key: FORCE_HTTPS
        value: "true"
      - key: PUBLIC_URL
        sync: false
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
      - key: OPERATOR_PASSWORD
        sync: false
      # 進捗報告のAI生成に使用。未設定でも数字ベースの下書きにフォールバックする。
      - key: OPENAI_API_KEY
        sync: false
      # 使用モデル。未設定なら gpt-5.5。管理画面から利用可能なモデルIDを確認できる。
      - key: OPENAI_MODEL
        sync: false
